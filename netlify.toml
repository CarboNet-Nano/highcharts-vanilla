[build]
  functions = "api"
  publish = "public"

[[redirects]]
  from = "/api/ws/*"
  to = "/.netlify/functions/websocket"
  status = 200
  force = true

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["ws"]

[dev]
  framework = "#static"
  targetPort = 8888
